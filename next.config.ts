import type { NextConfig } from "next";

// El build valida los tipos de TypeScript (no se ocultan errores).
// ESLint se ejecuta como paso aparte con `npm run lint`.
const nextConfig: NextConfig = {};

export default nextConfig;
